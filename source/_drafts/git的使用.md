---
title: git的使用
tags:
---
虽然平常经常用 `git` ，但是几乎都是 `push` ，`pull` ，`add` ，`commit` 这样的简单指令，为了应对更复杂的需求，我记下了这一篇笔记。当然，已经有了很多现成的教程，包括这个[速查表](git的使用/github-git-cheat-sheet.pdf) 。

## 修改 `commit` 的信息
## 指定拉取远程仓库的版本
## 基于某一个版本创建分支
## 解决 `push` 可能发生的冲突
## `submodule` 的管理和使用
## 配置本地的 `git`
